@'
# AGENTS.override.md — Legacy Smart Stock

This directory is legacy/reference material only.

Do not modify files in this directory unless the user explicitly asks to work on Smart Stock.

The active project is Negocio-Autonomo at the repository root.

If you are trying to implement product features, UI routes, Prisma changes, dashboard pages, or MVP patches, go back to the repository root and work there.

Required check before editing anything:

```powershell
git rev-parse --show-toplevel
Get-Location