import { Card, CardContent, CardHeader } from "@/components/ui";

export default function DashboardChannelsPage() {
  return (
    <Card>
      <CardHeader><div className="text-lg font-semibold text-slate-900">Canales</div></CardHeader>
      <CardContent className="text-sm text-slate-600">Placeholder operativo listo para conectar WhatsApp, Instagram y Web sin bloquear la demo actual.</CardContent>
    </Card>
  );
}
