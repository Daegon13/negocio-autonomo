import { Card, CardContent, CardHeader } from "@/components/ui";

export default function DashboardBookingsPage() {
  return (
    <Card>
      <CardHeader><div className="text-lg font-semibold text-slate-900">Agenda / Reservas</div></CardHeader>
      <CardContent className="text-sm text-slate-600">Placeholder operativo listo para conectar disponibilidad, confirmaciones y calendario en Patch 04.</CardContent>
    </Card>
  );
}
